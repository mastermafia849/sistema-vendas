<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Painel - Guará Lub</title>
    <style>
        body { background-color: #121212; color: #fff; font-family: Arial, sans-serif; margin: 0; display: flex; flex-direction: column; height: 100vh; }
        .header { display: flex; justify-content: space-between; align-items: center; background: #1e1e1e; padding: 15px 30px; border-bottom: 1px solid #333; }
        .user-info { font-weight: bold; color: #ffc107; }
        .logout-btn { background: #dc3545; color: white; padding: 8px 16px; border: none; border-radius: 4px; text-decoration: none; font-weight: bold; }
        .logout-btn:hover { background: #c82333; }
        .content { flex: 1; display: flex; justify-content: center; align-items: center; padding: 20px; }
        iframe { width: 100%; height: 100%; border: none; border-radius: 8px; background: #1e1e1e; }
    </style>
</head>
<body>
    <div class="header">
        <div class="user-info">👤 Usuário: {{ usuario }}</div>
        <a href="/logout" class="logout-btn">Sair</a>
    </div>
    <div class="content">
        <iframe src="{{ link_painel }}" allowFullScreen="true"></iframe>
    </div>
</body>
</html>